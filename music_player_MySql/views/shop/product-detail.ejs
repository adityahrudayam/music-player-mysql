<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/product.css">
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
            <main class="centered">
                <%if(product){%>
                    <h1>
                        <%=product.title%>
                    </h1>
                    <a href="/admin/edit-product/<%=product.id%>?edit=true" class="btn">Edit</a>
                    <form action="/admin/delete-product" method="POST">
                        <input type="hidden" name="_csrf" value="<%=csrfToken%>">
                        <input type="hidden" value="<%=product.id%>" name="productId">
                        <button class="btn" type="submit">Delete</button>
                    </form>
                    <hr>
                    <h2>Repeat - <%=product.repeat%> <%if(product.repeat>1){%>times<%}else{%>time<%}%></h2>
                    <p>
                        <%=product.description%>
                    </p>
                    <audio id="music">
                        <source id="p1" src="/<%=product.audioUrl%>" type="audio/mpeg">
                    </audio>
                    <input type="hidden" id="repeatTimes" value="<%=product.repeat%>">
                    
                    <%} else{%>
                        <h1>Nothing as such in the PlayList!</h1>
                    <%}%>
            </main>
            <%- include('../includes/end.ejs') %>